version: '3.8'

services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile  # Adjust this if your Dockerfile is in a different location or has a different name
    container_name: fastapi-app
    ports:
      - "8000:8000"
    environment:
      # Add any environment variables your application needs here
      # EXAMPLE_VAR: "value"
    volumes:
      # Add a volume if you need to have persistent data or any external bindings
      # - ./data:/app/data